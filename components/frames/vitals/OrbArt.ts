// Passive Insight — a scrying orb mounted on a stand. Just two pieces: a
// true circle (the orb — radius stays circular, never stretched) sitting
// directly on a trapezium (the stand), meeting at the circle's widest
// point (its equator) so the trapezium's top edge is the full-diameter
// chord — the widest a plain circle+trapezium join can be — then tapering
// straight down to a flat foot. Flat foot (not a point) leaves room for a
// longer label than a sharp tip would.

export const ORB_VIEW_BOX = "1.4 -0.4 54.8 54.8";

export const ORB_PATH = "M3.8,27A25,25,0,1,1,53.8,27L42.8,52L14.8,52Z";
